name: Frontend Image CI (Staging Build)

on:
  push:
    branches:
      - test
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-image-ci.yml'

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image tag (SHA)
        run: echo "IMAGE_TAG=${GITHUB_SHA::7}" >> $GITHUB_ENV

      - name: Build Docker image
        run: |
          docker build \
            -f ./frontend/Dockerfile.release \
            -t ghcr.io/braianespanon/abacontex-frontend:${IMAGE_TAG} \
            -t ghcr.io/braianespanon/abacontex-frontend:staging \
            ./frontend

      - name: Push Docker image (SHA)
        run: |
          docker push ghcr.io/braianespanon/abacontex-frontend:${IMAGE_TAG}
          docker push ghcr.io/braianespanon/abacontex-frontend:staging