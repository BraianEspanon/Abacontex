interface Window {
  APP_CONFIG?: {
    API_URL: string;

    KEYCLOAK_URL: string;
    KEYCLOAK_REALM: string;
    KEYCLOAK_CLIENT: string;
    KEYCLOAK_REDIRECT_URI: string;

    ENVIRONMENT: string;
  };
}