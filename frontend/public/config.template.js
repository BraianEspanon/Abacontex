window.APP_CONFIG = {
  API_URL: "${VITE_API_URL}",
  KEYCLOAK_URL: "${VITE_KEYCLOAK_URL}",
  KEYCLOAK_REALM: "${VITE_KEYCLOAK_REALM}",
  KEYCLOAK_CLIENT: "${VITE_KEYCLOAK_CLIENT}",
  KEYCLOAK_REDIRECT_URI: "${VITE_KEYCLOAK_REDIRECT_URI}",
  ENVIRONMENT: "${VITE_ENVIRONMENT}"
};